% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initial_counts_helper.R
\name{create_initial_counts}
\alias{create_initial_counts}
\title{Create Initial Counts for MCMC Sampler}
\usage{
create_initial_counts(config_info, Np_target, sr_target = 1)
}
\arguments{
\item{config_info}{The mating configuration table, typically created by
\code{create_config_info()}.}

\item{Np_target}{The target total number of parents (males + females) for
the simulation.}

\item{sr_target}{The target sex ratio (males / females). Defaults to 1.0.}
}
\value{
A numeric vector representing the initial count of each mating
configuration type.
}
\description{
This function generates an initial counts vector, primarily
composed of 1:1 mating pairs, to serve as a starting point for the MCMC
sampler. This is a crucial first step for the Metropolis algorithm.
}
\examples{
# Create a sample config table and an initial counts vector
# config <- create_config_info(max_males_per_female = 3, max_females_per_male = 3)
# initial_counts <- create_initial_counts(config, Np_target = 100, sr_target = 1.0)
# print(initial_counts)
}
