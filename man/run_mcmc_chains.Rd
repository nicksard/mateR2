% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_mcmc_chains.R
\name{run_mcmc_chains}
\alias{run_mcmc_chains}
\title{Run Independent MCMC Chains and Assemble Convergence Diagnostics}
\usage{
run_mcmc_chains(
  Np_target,
  sr_target,
  mean_mates_target,
  max_males_per_female,
  max_females_per_male,
  n_chains = 4,
  seed = 1,
  n_iter = 2e+05,
  burn_in = 20000,
  thin = 10,
  ...
)
}
\arguments{
\item{Np_target}{The target total number of parents (males + females).}

\item{sr_target}{The target sex ratio (males / females).}

\item{mean_mates_target}{The target mean number of mates per individual.}

\item{max_males_per_female}{The maximum number of males a female can mate with.}

\item{max_females_per_male}{The maximum number of females a male can mate with.}

\item{n_chains}{Integer. Number of independent chains (default 4).}

\item{seed}{Integer. Base seed; chain \code{i} uses \code{seed + i - 1} (default 1).}

\item{n_iter, burn_in, thin}{MCMC length, burn-in and thinning interval, passed
to \code{\link{generate_map_table}}.}

\item{...}{Further arguments passed to \code{\link{generate_map_table}}, such
as \code{np_weight}, \code{sr_weight}, \code{mm_weight} and \code{decay_constant}.}
}
\value{
A list with:
\item{chains}{List of the per-chain \code{\link{generate_map_table}} results.}
\item{traces}{Named list of \code{coda::mcmc.list} objects, one per target
parameter (\code{Np}, \code{sr}, \code{mean_mates}).}
\item{diagnostics}{A \code{data.frame} of realised posterior means, percent
error against target, Gelman-Rubin \code{rhat} and summed \code{ess}.}
}
\description{
Runs several independent Metropolis-Hastings chains against the
same demographic targets from dispersed starting states, and returns both the
per-chain results and \code{coda} objects ready for Gelman-Rubin and
effective sample size diagnostics. Running the chains through one function
keeps seeding, dispersal, burn-in and thinning consistent across them.
}
\details{
Chain \code{i} uses seed \code{seed + i - 1}, so the whole ensemble is reproducible from
a single integer.

\code{\link{create_initial_counts}} is deterministic, so every chain would
otherwise begin from the identical state and the Gelman-Rubin statistic would
be uninformative. Each chain therefore jitters the warm start: active block
counts are multiplied by a uniform draw between 0.6 and 1.4 and rounded, with
a floor of one. That disperses the starting states while keeping them near the
target manifold, so no chain begins somewhere infeasible.

Traces are the post-burn-in portion of the recorded history, thinned by \code{thin}
and wrapped as \code{coda::mcmc} objects. Requires the coda package, which is
listed under Suggests.
}
\examples{
\dontrun{
res <- run_mcmc_chains(
  Np_target = 200, sr_target = 2, mean_mates_target = 2,
  max_males_per_female = 10, max_females_per_male = 10,
  n_chains = 4, n_iter = 50000, burn_in = 5000, thin = 10,
  np_weight = 70, sr_weight = 70, mm_weight = 70, decay_constant = -0.05
)
res$diagnostics
}

}
