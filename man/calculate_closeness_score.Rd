% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/closeness_score_helper.R
\name{calculate_closeness_score}
\alias{calculate_closeness_score}
\title{Calculate a Closeness Score}
\usage{
calculate_closeness_score(actual, target_value)
}
\arguments{
\item{actual}{The value being evaluated. Must be a positive, finite number.}

\item{target_value}{The target value for comparison. Must be a positive,
finite number.}
}
\value{
A numeric score (\verb{0 <= score <= 1}) representing the closeness.
Returns 0 if inputs are invalid.
}
\description{
This function calculates a score representing how close an
\code{actual} value is to a \code{target_value}. The score is defined as the minimum
of the ratio (\code{actual / target_value}) and its inverse. This ensures the
score is always between 0 and 1, with values closer to 1 indicating a
better match. The function guarantees a non-zero, non-negative output for
logarithmic stability in subsequent calculations.
}
\examples{
# A perfect match
calculate_closeness_score(100, 100)
# A good match
calculate_closeness_score(90, 100)
# A poor match
calculate_closeness_score(10, 100)
}
