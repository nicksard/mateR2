% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_info_helper.R
\name{create_config_info}
\alias{create_config_info}
\title{Create a Mating Configuration Information Table}
\usage{
create_config_info(max_males_per_female, max_females_per_male)
}
\arguments{
\item{max_males_per_female}{The maximum number of males a single female can
mate with.}

\item{max_females_per_male}{The maximum number of females a single male can
mate with.}
}
\value{
A data frame with columns: \code{Block} (e.g., "1:1"), \code{Males}, \code{Females},
and \code{Complexity_Diff}, which is used to penalize more complex mating
configurations in the MCMC algorithm.
}
\description{
This function generates a data frame that serves as a lookup
table for all possible breeding configurations (mate pairs) within a
defined system. It is a necessary input for the MCMC sampler.
}
\examples{
# Create a config table for a system where a single individual can have up to 3 mates
config_table <- create_config_info(max_males_per_female = 3, max_females_per_male = 3)
print(config_table)
}
