% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_realized_matrix.R
\name{plot_realized_matrix}
\alias{plot_realized_matrix}
\title{Plot a Realized Breeding Matrix}
\usage{
plot_realized_matrix(
  mat,
  title = "Realized Breeding Matrix",
  fill_label = "Mates"
)
}
\arguments{
\item{mat}{A numeric matrix representing the breeding population. Rows = males, Cols = females.}

\item{title}{A character string for the plot title. Default is "Realized Breeding Matrix".}
}
\value{
A \code{ggplot} object representing the matrix heatmap.
}
\description{
Visualizes a specific breeding matrix (Individual-by-Individual) as a heatmap.
Males are represented on the rows (Y-axis) and Females on the columns (X-axis).
Zero values (no mating) are colored white, while successful matings are
colored using a custom heat gradient.
}
\examples{
# Create a sample 10x10 matrix with sparse matings
# test_mat <- matrix(sample(c(0, 0, 0, 1, 2, 3), 100, replace=TRUE), nrow=10)
# plot_realized_matrix(test_mat)
}
