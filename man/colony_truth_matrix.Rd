% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_colony_dat.R
\name{colony_truth_matrix}
\alias{colony_truth_matrix}
\title{Rebuild a Mating Matrix from a Recovered COLONY Pedigree}
\usage{
colony_truth_matrix(truth, n_dads = NULL, n_mums = NULL)
}
\arguments{
\item{truth}{The \code{truth} element of a \code{\link{read_colony_dat}}
result, or the whole result.}

\item{n_dads, n_mums}{Optional dimensions. Default to the largest index seen,
which understates the matrix when a parent produced no offspring; supply
\code{dim(K)} to get a frame that lines up with the original.}
}
\value{
An integer matrix, rows males and columns females.
}
\description{
Turns the truth table returned by \code{\link{read_colony_dat}}
back into a mating matrix of full-sibling counts, so it can be compared
cell for cell against the \strong{K} that was exported.
}
\examples{
\dontrun{
dat <- read_colony_dat("COLONY2_1.DAT")
identical(colony_truth_matrix(dat, nrow(K), ncol(K)), K)
}

}
